//prvi zadatal provera parnosti/naparnosti brojeva od 1 - 10
for (x = 1; x <= 10; x++) {
	if (x % 2 != 0) {
		console.log("Broj " + x + " je neparan!");
	}
	else {
		console.log("Broj " + x + " je paran!");
	}
}